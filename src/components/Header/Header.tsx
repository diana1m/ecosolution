import Logo from '../Logo/Logo'
import styles from './Header.module.scss'

const Header = () => {
  return (
    <header>
      <div>
        <Logo />
      </div>
    </header>
  )
}

export default Header
